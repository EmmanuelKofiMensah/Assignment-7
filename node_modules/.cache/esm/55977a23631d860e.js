let Post;_631‍.x([["getAllPost",()=>getAllPost],["getSinglePost",()=>getSinglePost],["createPost",()=>createPost],["updatePost",()=>updatePost],["deletePost",()=>deletePost]]);_631‍.w("../models/Post",[["default",["Post"],function(v){Post=v}]]);

       const getAllPost = async (req, res) => {
  const posts = await Post.find();
  res.status(200).json({ posts });
};

       const getSinglePost = async (req, res) => {
  const { postId } = req.params;
  const post = await Post.findById(postId);
  res.status(200).json({ post });
};

       const createPost = async (req, res) => {
  const { title, content } = req.body;
  const post = await Post.create({ title, content, user });
  res.status(201).json({ post });
};

       const updatePost = async (req, res) => {
  const { postId } = req.params;
  const post = await Post.findByIdAndUpdate(postId, req.body, { new: true });
  res.status(200).json({ post });
};

       const deletePost = async (req, res) => {
  const { postId } = req.params;
  await Post.findByIdAndDelete(postId);
  res.status(200).json({ message: "Post deleted successfully." });
};
