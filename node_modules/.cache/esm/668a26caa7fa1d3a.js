let Schema,model;_a7f‍.x([["default",()=>_a7f‍.o]]);_a7f‍.w("mongoose",[["Schema",["Schema"],function(v){Schema=v}],["model",["model"],function(v){model=v}]]);

const postSchema = new Schema({
    title :{
        type: String,
        required: [true, "Post title is required"],
    },

    content : {
        type: String,
        required: [true, "Post content is required"],
    },

    user :{
        type : Schema.Types.ObjectId,
        ref : "User",
        required : [true, "Author is required"],
    },

    numberOfLikes :{
        type: Number,
        default: 0,
    },

    likedBy: [
        {
            type: Schema.Types.ObjectId,
            ref: "User"
        },
    ],
},
{
    timestamps: true,
}
);


_a7f‍.d(model("Post", postSchema));